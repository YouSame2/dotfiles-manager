- defaults:
    link:
      create: true # Automatically create parent directories if they don't exist

- link:
    ~/.config: .config
    ~/.testwindowsdir: testwindowsdir # this should only appear on windows

- clean: ["~"]
# example conf
# - defaults:
#     link:
#       relink: true

# - clean: ['~']

# - link:
#     ~/.tmux.conf: tmux.conf
#     ~/.vim: vim
#     ~/.vimrc: vimrc

# - create:
#     - ~/downloads
#     - ~/.vim/undo-history

# - shell:
#   - [git submodule update --init --recursive, Installing submodules]
